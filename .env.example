# Disposable local integration services only. The fixture creates random per-test passwords.
MERGEN_TEST_ADMIN_DSN_PG16=postgresql://postgres:mergen-test-only@127.0.0.1:55416/postgres
MERGEN_TEST_ADMIN_DSN_PG18=postgresql://postgres:mergen-test-only@127.0.0.1:55418/postgres
MERGEN_TEST_ADMIN_DSN=postgresql://postgres:mergen-test-only@127.0.0.1:55416/postgres

# Used only when invoking the reference application's database dependency directly.
MERGEN_EXAMPLE_DATABASE_URL=
