# Observability examples

`dashboard.json` is a vendor-neutral panel inventory and `alerts.yaml` contains
Prometheus-style example rules for an OpenTelemetry Collector/exporter that preserves
the documented metric name and labels. Adjust exporter name normalization and thresholds
from measured baseline data before production use.
