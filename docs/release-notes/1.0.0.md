# 1.0.0 release decision

Status: **not approved / not published**. This document is a bounded decision template.

Promotion requires two completed external deployments, completed independent review with
no unresolved critical/high issue, completed RC observation without a contract reset,
green real-runtime/compatibility/migration/package/archive/security gates, and trusted
publication from a protected `v1.0.0` tag. The machine readiness record currently fails
those external gates, so the package remains `0.11.0a1`.
